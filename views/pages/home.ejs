<%- include('../partials/header') %> 

    <section class="section home-section">
        <div class="text-center">
            <img src="<%= data[Math.floor(Math.random() * data.length)].photo %> " alt="home image">
        </div>
    </section>

    <%- include('../partials/footer') %> 